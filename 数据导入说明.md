# LeapLunar04 测试数据导入说明

## ⚠️ 重要提示

**我们提供了两个版本的SQL脚本：**

1. **`test_data.sql`** - 完整版本
   - 包含所有测试数据
   - 使用普通 INSERT 语句
   - ❌ 如果ID冲突或邮箱重复，可能会报错
   - ✅ 适合在**全新数据库**中使用

2. **`test_data_safe.sql`** - 安全版本 ⭐ **推荐**
   - 使用 `INSERT IGNORE` 语句
   - ✅ **不会覆盖现有数据**
   - ✅ 如果邮箱已存在，会自动跳过
   - ✅ 适合在**已有数据的数据库**中使用

> 💡 **建议：如果您已经有账号和数据，请使用 `test_data_safe.sql`**

---

## 📋 数据概览

本测试数据包含了一套完整的、真实的创意作品分享平台数据，包括：

### 用户数据 (5个账号)
- **designer1@example.com** - 设计师小明 (UI/UX设计师)
- **creator2@example.com** - 创意达人Lisa (视觉设计师、插画师)
- **artlover@example.com** - 艺术探索者 (品牌设计师)
- **photographer@example.com** - 光影摄影师 (商业摄影师)
- **admin@leaplunar.com** - 平台管理员 (管理员权限)

> 🔑 所有账号的密码都是：**password123**

### 内容数据
- ✍️ **8篇图文作品** - 涵盖UI设计、插画、品牌、摄影等多个领域
- 🎬 **5个视频作品** - 包括教程、延时创作、Vlog等类型
- 📦 **6个设计资源** - UI Kit、素材包、预设、模板等
- 🏷️ **6个作品分类** - UI设计、插画艺术、品牌设计、摄影作品、动效设计、平面设计

### 互动数据
- 👥 **12条关注关系** - 用户之间相互关注
- 💬 **~30条评论** - 分布在文章、视频、文件中
- ❤️ **~20条点赞** - 用户对作品的点赞
- ⭐ **~20条收藏** - 用户收藏喜欢的作品
- 🔔 **12条通知消息** - 关注、点赞、评论、收藏通知

---

## 🚀 导入步骤

### ⚠️ 在执行前，请先备份数据库！

```sql
-- 备份数据库命令（MySQL）
mysqldump -u your_username -p your_database_name > backup_$(date +%Y%m%d_%H%M%S).sql
```

### 方式一：使用数据库管理工具（推荐）

#### 1. 使用 MySQL Workbench
```bash
1. 打开 MySQL Workbench
2. 连接到您的数据库
3. 点击菜单 File -> Run SQL Script
4. 选择 test_data.sql 文件
5. 点击 Run 执行
```

#### 2. 使用 Navicat
```bash
1. 打开 Navicat
2. 连接到您的数据库
3. 右键数据库 -> Execute SQL File
4. 选择 test_data.sql 文件
5. 点击 Start 执行
```

#### 3. 使用 phpMyAdmin
```bash
1. 登录 phpMyAdmin
2. 选择您的数据库
3. 点击顶部 "导入" 标签
4. 选择文件：test_data.sql
5. 点击 "执行" 按钮
```

### 方式二：使用命令行

#### MySQL 命令行
```bash
# 进入MySQL命令行
mysql -u your_username -p

# 选择数据库
USE your_database_name;

# 执行SQL文件
source F:/zhangjiangwei/DEMO/DEMO-2/test_data.sql;

# 或者在外部直接执行
mysql -u your_username -p your_database_name < F:/zhangjiangwei/DEMO/DEMO-2/test_data.sql
```

---

## ⚠️ 注意事项

### 1. 数据库兼容性
- 本脚本使用 MySQL 语法编写
- 如果使用 PostgreSQL，需要调整部分语法：
  - `NOW()` → `CURRENT_TIMESTAMP`
  - `INTERVAL 5 DAY` → `INTERVAL '5 days'`
  - 自增ID配置方式不同

### 2. 表结构要求
确保您的数据库中已经创建了以下表：
- `users` - 用户表
- `categories` - 分类表
- `articles` - 文章表
- `videos` - 视频表
- `files` - 文件表
- `follows` - 关注关系表
- `comments` - 评论表
- `likes` - 点赞表
- `favorites` - 收藏表
- `notifications` - 通知表

### 3. 字段对应关系
请确保表结构中的字段名与SQL脚本中的字段名一致，主要字段包括：
- 用户表：`email`, `password`, `nick_name`, `avatar`, `gender`, `birthday`, `signature`, `role`, `status`
- 文章表：`user_id`, `category_id`, `title`, `summary`, `content`, `cover_image`, `view_count`, `like_count`
- 视频表：`user_id`, `category_id`, `title`, `description`, `video_url`, `duration`
- 文件表：`user_id`, `category_id`, `title`, `file_name`, `file_type`, `file_size`, `file_url`

### 4. 密码加密
- 所有用户密码都使用 bcrypt 加密
- 加密后的值：`$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy`
- 对应明文密码：`password123`
- 如果您的后端使用不同的加密方式，请修改password字段的值

### 5. 文件URL
- 脚本中的图片和文件URL使用的是示例链接
- 图片使用 Unsplash 的真实图片链接（可直接访问）
- 视频和文件下载链接是示例URL（需要替换为实际地址）

---

## 🧪 验证数据

导入完成后，可以执行以下SQL查询验证：

```sql
-- 检查用户数量
SELECT COUNT(*) as user_count FROM users;
-- 应该返回：5

-- 检查文章数量
SELECT COUNT(*) as article_count FROM articles;
-- 应该返回：8

-- 检查视频数量
SELECT COUNT(*) as video_count FROM videos;
-- 应该返回：5

-- 检查文件数量
SELECT COUNT(*) as file_count FROM files;
-- 应该返回：6

-- 检查分类数量
SELECT COUNT(*) as category_count FROM categories;
-- 应该返回：6

-- 查看用户列表
SELECT id, email, nick_name, role FROM users;

-- 查看作品列表
SELECT a.title, u.nick_name as author, c.name as category 
FROM articles a 
JOIN users u ON a.user_id = u.id 
JOIN categories c ON a.category_id = c.id;
```

---

## 🔍 测试场景建议

### 1. 用户功能测试
- 使用任意账号登录：`designer1@example.com` / `password123`
- 浏览首页，查看其他用户的作品
- 关注/取消关注其他创作者
- 点赞、评论、收藏作品

### 2. 创作者功能测试
- 登录后进入"我的作品"
- 查看自己发布的文章、视频、文件
- 编辑或删除自己的作品
- 查看作品的浏览量、点赞数等统计

### 3. 社交功能测试
- 查看关注的人的动态
- 查看粉丝列表和关注列表
- 发送私信（如果有此功能）
- 查看通知消息

### 4. 管理员功能测试
- 使用管理员账号登录：`admin@leaplunar.com` / `password123`
- 进入管理后台
- 管理用户、作品、分类等
- 审核举报内容

---

## 📝 自定义数据

如果需要添加更多数据，可以参考 `test_data.sql` 中的格式，修改以下内容：

1. **添加用户**：修改 users 表的INSERT语句，注意密码需要bcrypt加密
2. **添加作品**：在 articles/videos/files 表中添加新记录
3. **调整数量**：复制现有INSERT语句，修改具体内容
4. **修改时间**：调整 `NOW() - INTERVAL X DAY` 中的天数

---

## ❓ 常见问题

### Q: 导入时报错 "Duplicate entry for key 'PRIMARY'"
**A:** 可能是表中已有数据。建议先清空相关表，或修改SQL中的ID值。

### Q: 密码无法登录
**A:** 确认后端使用的是bcrypt加密，且salt轮数为10。如果不是，需要重新生成密码hash。

### Q: 图片无法显示
**A:** Unsplash图片需要网络访问。如果在内网环境，请替换为本地图片URL。

### Q: 用户关注数不对
**A:** 执行SQL脚本末尾的UPDATE语句，更新用户的follower_count和following_count。

---

## 📧 技术支持

如有问题，请检查：
1. 数据库版本是否兼容
2. 表结构是否与SQL脚本匹配
3. 字段类型和长度是否足够
4. 外键约束是否正确设置

---

**祝您使用愉快！🎉**
